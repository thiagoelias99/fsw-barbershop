/** @type {import('next').NextConfig} */
const nextConfig = {
  images:{
    domains: ['utfs.io']
  },
  i18n: {
      locales: ['pt-BR'],
      defaultLocale: "pt-BR",
    },
}

export default nextConfig;